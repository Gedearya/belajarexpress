<!DOCTYPE html>
<html>
  <head>
    <title>Express Arya</title>
    <style>
      body {
        background: green;
        font-family: verdana;
        color: white;
        padding: 150px;
      }
      h1 {
        font-size: 72px;
        text-transform: uppercase;
        letter-spacing: 2px;
        text-align: center;
      }
      p {
        font-size: 32px;
        text-align: center;
      }

      ul,
      li {
        text-align: center;
      }
    </style>
  </head>

  <body>
    <%- include('parsial/header') %>
    <p>
      <img
        src="http://localhost:3001/assets/images/<%= product.image_filename %>"
        alt=""
      />
    </p>
    <p>Nama makanan : <%= product.name %></p>
    <p>Harga makanan : <%= product.price %></p>
    <p>Deskripsi makanan : <%= product.description %></p>
  </body>
</html>
